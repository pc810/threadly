import { ClientCommunityWidgetWrapper } from "@/components/community";
import { ReactNode } from "react";

export default function ModWidgetRoot({ children }: { children: ReactNode }) {
  return children;
}
